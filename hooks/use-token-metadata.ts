// Stub — Jupiter metadata fetch removed. Token name/symbol come from Metaplex
// on-chain data fetched in getAllTokens() and passed via item props / route params.
export const useTokenMetadata = (_mint: string) => ({ metadata: null, loading: false })
